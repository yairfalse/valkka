[package]
name = "kanni_git"
version = "0.1.0"
authors = []
edition = "2021"

[lib]
name = "kanni_git"
crate-type = ["cdylib"]

[dependencies]
rustler = "0.36"
git2 = "0.20"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
